# Equilibrium with Endogenous Pool Return: Proof Notes

*Extension of the insurance paper's Theorem 1 to a pool return that declines
toward the market rate as capital crowds in. Analytical statements here;
computational verification in `prove_equilibrium.py` (VERDICT output).*

## Setup

Replace the constant pool return with scarce alpha:

```
r_pool(C) = r_market + (r_pool0 - r_market) * K / (K + C),        (E1)
R(C)      = r_pool(C) * C     (total annual pool revenue),
```

where `C = C_LP + sum_i CC_i (+ C_S)` is total pool capital and `K > 0` the
alpha capacity. Protocol i's yield income becomes

```
Y_i(CC_i; CC_-i, C_LP) = (1-gamma)(1-phi) * R(C_-i + CC_i) * w_i,
w_i = CC_i / (S_-i + CC_i),      S_-i = sum_{j != i} CC_j,        (E2)
```

with `C_-i = C_LP + S_-i`. Everything else (coverage Eq. 2, utilization
Eq. 3, cap Eq. 5, yield share Eq. 8) is unchanged from the paper.

## Solution concept (restated after the adversarial audit, 2026-07-18)

Protocols are **aggregate-takers**: each optimizes taking the mechanism
aggregates — the yield share `gamma`, pool revenue `gross_rev`, prudential
scaling `cap_scale`, and the hazard index — as given, analogous to
price-taking in competitive equilibrium. Equilibrium additionally requires
that the aggregates are the ones generated by the chosen profile. The
implemented best response and the epsilon certificate are exact for THIS
game. In the full game, a deviating protocol also moves the aggregates;
the distance between the two concepts is MEASURED, not assumed:
recomputing all aggregates at the deviated profile, the largest-collateral
protocols gain at most ~1e-4 $M (~$100) per quarter (THEOREM1_FULLGAME in
`prove_equilibrium.py`). With heavy-tailed TVL (Pareto 1.2) this
aggregate-taking error is not uniformly O(1/n) — it is reported per run
rather than bounded analytically.

## Step 1 — Properties of R (symbolically verifiable)

**Fact 1.** Under (E1): R is continuous, strictly increasing, strictly
concave, with `R(0) = 0` and `R(C)/C -> r_market` as `C -> inf`.

*Proof.* `R(C) = r_market*C + s0*K*C/(K+C)` with `s0 = r_pool0 - r_market > 0`.
The first term is linear; the second has second derivative
`-2*s0*K^2/(K+C)^3 < 0`. QED (SymPy check in `prove_equilibrium.py`).

Any alternative decay satisfying Fact 1 works; nothing below uses the
hyperbolic form beyond these properties.

## Step 2 — The key lemma (own-payoff concavity of the yield term)

The obstacle to existence is that (E2) is a *product* of two terms moving
with own collateral. The lemma shows the aggregate-revenue concavity is
enough:

**Lemma 1.** If R is nondecreasing, concave, and `R(0) = 0`, then
`f(c) = R(C_LP + S_-i + c) * c/(S_-i + c)` is concave in `c >= 0`.

*Proof.* Write `A = C_LP + S_-i >= S = S_-i`, `w(c) = c/(S+c)`. Then
`f'' = R''w + 2R'w' + Rw''` with `w' = S/(S+c)^2 > 0`,
`w'' = -2S/(S+c)^3 < 0`. Concavity of R through the origin gives the
average-exceeds-marginal property `R'(A+c) <= R(A+c)/(A+c) <= R(A+c)/(S+c)`.
Hence

```
2R'w' <= 2 * [R/(S+c)] * S/(S+c)^2 = -R w'',
```

so `f'' <= R'' w <= 0`. QED.

Intuition: a protocol adding collateral gains pool share (concave: `w'' < 0`)
but dilutes the pool's per-dollar alpha (`R' <= R/C`); the dilution
dominates the convexifying cross term. The lemma also covers the constant
`r_pool` case (R linear: `R' = R/C`, inequalities weak), so existence was
never the issue in the fixed-alpha game — the carry trade was a statement
about *levels* (unbounded aggregate demand up to the wealth cap). Strictly
concave R additionally caps the aggregate (Step 4).

**Symbolic proof (hyperbolic R) — achieved by the adversarial audit.**
Substituting `A = S + d` (d >= 0), `f''` reduces to a rational function
whose denominator factors into positive terms `(S+c)^3 (K+S+c+d)^3` and
whose expanded numerator is a polynomial in `(c, S, d, K, s0, r_m)` with
ALL coefficients nonpositive — hence `f'' <= 0` identically on the
admissible domain. Reproduced in `prove_equilibrium.py` (LEMMA1, exact).
Caveats: general concave R needs a C^2 mollification step (routine); and
under the code convention `w(0) = 0` the map f jumps upward at `c = 0+`
when `S = 0` — f remains concave on `[0, inf)` and `_golden_cc` probes the
`C_C = 0` endpoint explicitly, so the optimizer is unaffected.

**Where Lemma 1 binds.** In the implemented (aggregate-taking) best
response, `gross_rev` is held fixed, so the yield term's concavity in own
`C_C` follows from `w'' < 0` alone; Lemma 1 is the stronger statement for
the game in which the deviator's collateral moves `R(C)` itself, showing
concavity survives that internalization. Concavity through the remaining
full-game channels — own `C_C` moving `gamma` (via the fair split) and
`cap_scale` (via utilization) — is NOT established; this is why the
full-game deviation check uses a dense grid scan rather than a local
optimizer, and why Theorem 1' below is stated for the aggregate-taking
game.

## Step 3 — Existence (Theorem 1', mirroring the paper's Theorem 1)

**Theorem 1'.** Consider the AGGREGATE-TAKING stage game with strategy
sets `CC_i in [0, W_i]`, `h_i in [0, h_max]`, `C_LP in [0, W_LP]`,
speculators competitive risk-neutral (prices = induced attack
probabilities), pool return (E1), and protocols optimizing against the
aggregates generated by the profile. If, for fixed rival strategies and
aggregates, each protocol payoff is quasi-concave in own `(CC_i, h_i)`
and the LP payoff quasi-concave in `C_LP` (A1'), then an equilibrium
(profile + self-consistent aggregates) exists.

*Proof sketch.* Strategy sets are nonempty, compact, convex. Payoffs are
continuous: `r_pool(.)` is continuous (E1), coverage, gamma (clipped),
and the attacker best-response objects `p_q(V, h)`, `b*(V, h)` are
continuous in `h` by the maximum theorem (the attacker's problem has
continuous objective and compact effort domain, and its solution enters
protocol payoffs only through continuous functionals). Under (A1'),
best-response correspondences are nonempty, convex-valued, u.h.c. (Berge);
Kakutani on the product delivers a fixed point, exactly as in the paper's
Appendix C. QED.

**Discharging (A1') instead of assuming it.** In the aggregate-taking
game (`gamma`, `gross_rev`, `cap_scale` fixed during optimization — the
solution concept, not a hidden assumption), the protocol payoff in own
collateral is a sum of: (i) expected-payout term — concave in CC
(composition of the concave coverage `mu*CC^theta`, theta < 1, with the
concave nondecreasing `E[min(., L)]`); (ii) forfeiture and opportunity
cost — linear; (iii) the yield term — concave since `w'' < 0` (and by
**Lemma 1** even if the deviator's effect on `R` is internalized). Sums
of concave functions are concave, so the payoff is concave (hence
quasi-concave) in `CC_i` for every `h_i` — *no weak-feedback assumption
needed for the collateral margin*, an improvement over assuming (A1)
wholesale. (Full-game quasi-concavity through the `gamma`/`cap_scale`
channels remains open; the full-game deviation gap is measured instead.) The `h`-margin (through `p_q(h)`, `b*(h)`, `xi(h)`) is not
globally concave in general; in the implementation `h` lives on a finite
grid, so the existence argument applies to the finite-action mixed
extension (Nash 1951) or, in practice, we verify ex post that the computed
pure profile admits no profitable unilateral deviation on the full grid
(`deviation_check`, VERDICT in `prove_equilibrium.py`).

Note on the aggregate: given the equilibrium yield share `gamma` and cap
scale, the collateral subgame is an *aggregative game* (payoffs depend on
own CC and the aggregate S). With strictly concave own-payoffs, best
responses are single-valued and continuous, so the aggregate best-response
map `S -> sum_i BR_i(S)` is continuous on `[0, sum_i W_i]` and Brouwer
gives the fixed point directly — this is the computational route the code's
damped iteration follows.

## Step 4 — Carry-trade extinction (the economics of the extension)

**Proposition E2 (corrected after the adversarial audit).** Let
`rbar(C) = [R(C) + F(C)] / C` be the all-in per-dollar pool revenue rate
(base return plus speculator-fee inflows; in the implementation
`rbar -> r_market + fee_base` as `C -> inf`, NOT `r_market`). At any
interior equilibrium with small own price impact, the marginal annual
value of collateral satisfies

```
(1-phi) * (1-gamma*) * (C*/S*) * rbar(C*) + MB_cov,i
      =  r_market + 4*p_i + o(1),                                  (E3')
```

where `(1-gamma*) C*/S*` is the protocols' revenue share per dollar of
posted collateral, `MB_cov,i >= 0` the marginal coverage/risk-utility
benefit, and `4*p_i` the annualized forfeiture premium. The earlier draft
substituted `(1-gamma*) ~ S*/C*` (pure fair split), which the audit
refuted quantitatively: with the paper's App.-B blend (`eta_blend = 0.5`)
and `gamma_raw >> gamma_fair`, the measured factor is ~0.6, and the fee
wedge is first-order. Hence:

1. The pure carry motive dies where `(1-phi)(1-gamma*)(C*/S*) rbar(C*)`
   falls to `r_market + 4*p_i - MB_cov,i` — an implicit bound on `C*`;
   with fees, per-dollar revenue does not decay all the way to
   `r_market`, and in the present calibration a majority of protocols sit
   at the wealth cap `cc_tvl_cap * TVL`, so their collateral is pinned by
   wealth and forfeiture risk rather than alpha decay.
2. `C*` is nondecreasing in capacity K (numerical comparative static,
   verified in the K sweep), and as `K -> inf` the fixed-alpha carry
   regime re-emerges.
3. (E3') is testable in the simulation: at K = 500 the measured marginal
   yield (0.0549/yr) matches the decomposition
   `(1-phi)(1-gamma*)(C*/S*) rbar(C*)` (0.0553/yr) to within 1%.

*Proof sketch.* Differentiate the protocol payoff in `CC_i` at an
interior optimum; the aggregate-taking marginal yield per collateral
dollar is `(1-gamma*)(1-phi) * gross_rev / S*` (own price impact enters
at `O(c_i/C)` by Lemma 1's average/marginal bound); equate to marginal
cost. QED (numerical checks: K sweep + the E3' decomposition above).

## Step 5 — Proposition C: the carry trade as a security subsidy

The spread `s0 = r_pool0 - r_market` is not only a capital-attraction
lever; with forfeiture it changes the protocol's SECURITY incentive.
Write the aggregate-taking protocol payoff (quarterly, fixed aggregates)

```
U(CC, h) = (1+rho) p(h) A(CC, h) + (1 - p(h)) G w(CC)
           - p(h) CC 1{forf} - (r_m/4) CC - (c_h/4) h V - p(h) Ebar b(h) V,
```

with `A = E[min(cov(CC,h), eps b V)]` the expected payout,
`G = (1-gamma)(1-phi) gross_rev` the protocol-side revenue pot,
`w(CC) = CC/(S_-i + CC)`, and `p(h)` the attacker's response. Decompose
the marginal value of collateral as

```
dU/dCC = chi(CC) + M(CC),
chi(CC) = (1-p) G w'(CC) - r_m/4 - p 1{forf}     (carry margin)
M(CC)   = (1+rho) p A_CC >= 0                     (insurance margin).
```

`chi` is the return to collateral unrelated to risk transfer — the carry
trade. `G` is increasing in the all-in spread (`gross_rev` scales with
`r_pool(C) + fees`), so `chi > 0` regions exist only when the pool
out-earns the market.

**Proposition C.** In the aggregate-taking stage game with forfeiture:

*(i) Regimes.* Protocol i posts to the wealth cap iff
`chi(cap) + M(cap) >= 0` (carry regime); otherwise the optimum is
interior and solves `chi + M = 0` (insurance regime). With s0 = 0 and no
fees, `chi < 0` for every CC > 0, and collateral is pinned by the
insurance motive alone.

*(ii) Advantageous selection.* `d chi / d p = -(G w' + 1{forf}) < 0`:
the carry margin strictly decreases in own attack probability. Carry
capital therefore selects toward the SAFEST protocols (low p from low V
or high h) — the opposite of adverse selection: the protocols most
likely to trigger forfeiture value the carry least.

*(iii) Carry-security complementarity.* The cross-partial splits into a
deterrence channel and an insurance channel:

```
d2U/dCC dh = -p_h [ G w'(CC) + 1{forf} ]     > 0   (deterrence channel)
             + p_h (1+rho) A_CC + (1+rho) p A_CC,h  <= 0 (insurance channel)
```

since `p_h < 0` and `A_CC,h <= 0` (defense lowers losses, hence the
marginal value of coverage). The deterrence channel — security protects
both the forfeitable collateral AND the (1-p)-contingent carry income —
makes collateral and security complements; the insurance channel makes
them substitutes. Whenever the carry motive dominates (large s0, cap
posters with saturated coverage), the NET cross-partial is positive:
larger spread => more collateral => stronger marginal security incentive.

*(iv) Equilibrium spread condition.* With endogenous alpha the aggregate
adjusts until the marginal poster's `chi + M = 0`, i.e. E3'; shrinking
s0 or K contracts carry collateral and, by (iii), equilibrium security.

*Proof.* (i)-(iii) are direct calculus on U; signs follow from `p_h < 0`,
`w' > 0`, `A_CC >= 0`, `A_CC,h <= 0`. (iv) is E3'. QED.

Proposition C converts the audit's "collateral-as-carry-trade" wrinkle
into a design statement: **the pool spread is a security subsidy routed
through forfeiture, and its incidence is advantageous** — it explains
both numerical findings at once (protocols yield-farm the pool under
fixed alpha; equilibrium security falls when alpha is compressed:
h* 1.15 -> 0.86, hacks +50% at K = 250). Numerical verification:
`prove_equilibrium.py` (PROPC) checks the FOC split at the computed
equilibrium, the selection ranking (cap posters have lower mean p than
interior posters), and the sign pattern of the two cross-partial
channels by finite differences.

## Computation: the four-phase stage solver and its certificate

`solve_stage` (game.py) computes each quarter's equilibrium in four phases:
1. damped simultaneous best-response iteration (global stability);
2. undamped polish on self-consistent aggregates, keeping the profile with
   the smallest certified epsilon;
3. sequential (Gauss-Seidel) refinement over protocols with positive gain —
   breaks the 2-cycles that simultaneous best response produces among
   near-indifferent protocols;
4. exhaustive enumeration over small flip sets (<= 5 protocols) — finds an
   exact pure profile among the cycling candidates if one exists.

The reported `eps_stage` is the maximum unilateral (h, C_C) utility gain at
the accepted profile under its own aggregates — the Nash criterion itself,
with the C_C dimension exact by Lemma 1 (concave => golden section exact).
Empirically ~90% of epochs certify at eps < 1e-9 $M; the remainder plateau
at eps ~ 1e-4 $M (a few hundred dollars per quarter) where phases 3-4 find
no exact profile — consistent with genuine non-existence of exact
pure-strategy equilibria in the finite-h game at those states.

## What the computational script certifies (`prove_equilibrium.py`)

| Claim | Method | Verdict semantics |
|---|---|---|
| Fact 1 (R concave, increasing) | SymPy signs of R', R'' | PROVED (exact) |
| Lemma 1 (yield-term concavity) | EXACT symbolic (A = S + d numerator-coefficient proof) + independent numerical search | PROVED (exact for hyperbolic R) |
| Theorem 1'A (exact aggregate-taking pure Nash every epoch) | per-epoch eps certificate + independent dense-scan deviation check | PROVED only if every epoch certifies at eps < 1e-9; otherwise INCONCLUSIVE — exact pure equilibria need not exist in finite games |
| Theorem 1'B (path is a certified eps-equilibrium, aggregate-taking) | measured eps bound | PROVED with the measured bound reported |
| Theorem 1'C (distance to FULL-game Nash) | dense deviation scan with ALL aggregates recomputed, top-collateral protocols | PROVED as a measured bound (~1e-4 $M/quarter) |
| Prop. E2' (extinction, corrected E3') | K sweep + marginal-yield decomposition | numerical comparative statics |

Limitations: Lemma 1 is exact (hyperbolic family; general concave R via
mollification); analytic existence covers the mixed extension (Nash 1951)
and the continuous-strategy aggregate-taking game (Berge+Kakutani);
full-game quasi-concavity through the gamma/cap_scale channels is open;
E2' is verified at n = 500 with heavy-tailed TVL.

## Step 6 — Proposition D: forfeiture decouples deterrence from indemnity
   (added 2026-07-23 for the journal paper; certified by
   `run_deductible_frontier.py`)

**Referee attack this answers.** "Forfeiture is a 100%-deductible plus
negligence exclusion — classical moral-hazard instruments (Shavell 1979,
Holmstrom 1979) already cover this." The answer is a *Tinbergen* point: with
indemnity-side instruments alone, deterrence and risk transfer are the same
dial turned in opposite directions; forfeiture is a second, independent
instrument, and under adversarial risk the missing instrument is costly in a
way it is not under nature-generated risk.

### Environment (as in Setup, plus one instrument)

Augment the mechanism with a coinsurance rate d in [0,1]: the claim paid on
loss L is (1-d)·min(cov, L). Family A ("indemnity-only contracts") is the
set {d in [0,1], forfeiture off}: every classical indemnity-side instrument
(deductible, coinsurance, coverage denial, negligence exclusion) is a
state-contingent choice of d; a negligence exclusion is d(h) = 1{h < h_min}.
Limited liability / the indemnity principle cap what any such contract can
do to the insured on a hack: it can *withhold at most the claim itself*.
The forfeiture mechanism (d = 0, forfeiture on) additionally transfers the
posted collateral CC to the pool on a hack.

### Lemma A (indemnity irrelevance to the attacker, structural)

Holding the defender profile (h, CC) and aggregates fixed, the attacker's
problem max_e beta(e,h)·V·eps_mean − c(e,V) contains no indemnity-side
object: e*, b*, pi*, and the execution probability are independent of d and
of the claim rule. *Proof: inspection — claims are transfers between pool
and protocol after extraction; the attacker's payoff depends on (e, h, V)
only.* (In code: `attacker_best_response` takes no mechanism argument.)
Hence EVERY effect of insurance design on hack arrivals runs through the
defender's induced choices (h, CC). Instruments differ only in which
defender margins they can move.

### Lemma B (the indemnity frontier)

Within family A, the protocol's on-hack utility differential between
complying (high h) and shirking (low h) is bounded by the withheld claim:

    Delta_A(h, d)  <=  E[min(cov, L)]              (indemnity bound)

so the strongest indemnity-only discipline is full denial, whose penalty is
capped by the *loss itself* — and it disciplines by *destroying risk
transfer*. Formally, let s(d) = (1-d)·E[min(cov,L)]/E[L] be the indemnified
share and h*(d) the equilibrium security. Discipline requires d high
(s low); transfer requires d low (s high): the attainable set of family A
traces a frontier s -> h*(s) that is decreasing wherever coverage crowds
out effort (the moral-hazard region identified in the no-forfeiture
counterfactual, h* 1.16 -> 0.18). One instrument, two targets.

With forfeiture the on-hack penalty is Delta_F = Delta_A(h, 0) + CC:
decoupled from the claim, financed by the collateral the carry pays
protocols to post (Prop. C), and unbounded by the indemnity principle.
The mechanism can therefore pay the claim IN FULL and discipline h through
CC — a point (s ~ s(0), h* ~ h*_no-ins) outside the family-A frontier.

### Proposition D (decoupling / deterrence dominance)

 (i)  [Frontier] Within family A, equilibrium security h*(s) is decreasing
      in the indemnified share s over the moral-hazard region; and by
      Lemma A + the attacker's best response (beta decreasing in h, pi*
      increasing as h falls), hack intensity is increasing in s — the
      attacker AMPLIFIES the frontier's slope relative to exogenous risk.
 (ii) [Pareto escape] The forfeiture mechanism attains a (transfer,
      security) pair outside the family-A attainable set: no indemnity-only
      contract achieves both the mechanism's indemnified share and its
      security simultaneously, and the mechanism's security strictly
      exceeds the family-A frontier evaluated at the mechanism's transfer
      level. The mechanism does NOT maximize the indemnified share per se:
      concave coverage caps whale claims by design (capital efficiency),
      so its share sits ~12pp below the full-indemnity contract's — the
      escape is in the pair, not in either coordinate alone. (Certified
      2026-07-23, strict criteria, both PASS: frontier Spearman rho = -1.00,
      p < 1e-4; at the mechanism's share s = 0.876 the interpolated family-A
      frontier gives h = 0.40 (~8 hacks/yr); the mechanism delivers
      h* = 1.15 — the no-insurance security level — at 2.1 hacks/yr;
      escape gap 0.75 in h against tolerance 0.05.)
 (iii)[Adversarial amplification] The value of the second instrument is
      larger under a strategic attacker than under nature: replacing the
      endogenous attacker with a fixed hack hazard (the p_q profile of the
      forfeiture equilibrium) shrinks the security gap between forfeiture
      and the best family-A contract, because the extensive-margin response
      (participation pi* > 0 for more targets as h falls) is shut down.

Status: (i) and (ii) are certified numerically on the calibrated game by
`run_deductible_frontier.py` (strict criteria: Spearman rho < 0 with
p < .05 for the frontier; corner-wise weak dominance + strict dominance at
both corners for the escape — see `outputs/results_decoupling.json`).
(iii) is stated as the mechanism-decomposition claim; its clean analytical
version (comparative static of the frontier slope in the attacker's
responsiveness d(beta)/dh, and in the participation elasticity via F_bar)
is the target theorem for the paper's stylized model section. In the
continuum-of-protocols limit the frontier statement should follow from the
aggregate-taking apparatus of Step 3 plus monotone comparative statics of
the per-protocol problem in d (the payout term (1-d)·p·E[min(cov,L)] is
supermodular in (-d, h) wherever coverage binds); this is the analytical
gap to close for the paper — flagged honestly, as with E2'.

### Corollary D1 (why the carry is what makes the hostage postable)

The bonding critiques (Dickens-Katz-Lang-Summers 1989; Williamson 1983)
say forfeitable bonds fail in practice because (a) agents cannot or will
not post them and (b) the bond-holder has an incentive to seize them
falsely. In this mechanism (a) is answered by Prop. C: the carry
(r_pool - r_market) pays protocols to post CC, and the participation
margin chi > 0 prices the expected forfeiture; (b) is answered by the
parametric on-chain trigger: forfeiture executes on a verifiable hack
event, not on a discretionary negligence finding — there is no adjudicator
to corrupt. Both answers are DeFi-specific: a traditional insurer can
neither pay clients to post seizable capital at above-market yield nor
commit to non-discretionary seizure. This is the paper's "why blockchain"
argument in contract-theoretic form.

## Provenance

Lemma 1's symbolic proof route, the aggregate-taking restatement, and the
E3' correction come from an independent adversarial referee pass
(prover-verifier protocol in the sense of Koren, arXiv:2606.22337) run on
2026-07-18 against a draft of these notes; the referee's numerical
counter-experiments (true full-game deviations ~1e-4 $M vs. the original
certificate's 1e-12) forced the solution-concept restatement.
Step 6 was added 2026-07-23 following the five-strand literature sweep for
the journal paper; the "extreme deductible" framing attack it answers was
identified in the deterrence/collateral strand (see
`JournalPaper/positioning_memo.md` in the research folder).
